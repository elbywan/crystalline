name: Build

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [master]
  workflow_dispatch:
  schedule:
    - cron: "0 0 10 * 6"

jobs:
  macos:
    strategy:
      matrix:
        include:
          - runner: macos-15
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2
      - name: Install Crystal
        env:
          HOMEBREW_NO_INSTALL_UPGRADE: 1
          HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: 1
        run: brew update && brew install crystal || true
      - name: Build the binary
        run: |
          # Homebrew ships LLVM as a versioned, keg-only formula (`llvm@22` for
          # the current Crystal): there is no `llvm` alias to ask for.
          export LLVM_CONFIG="$(ls -d "$(brew --prefix)"/opt/llvm*/bin/llvm-config 2>/dev/null | sort -V | tail -n 1)"
          shards build crystalline --release --no-debug --stats --progress --ignore-crystal-version
      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: crystalline_${{ matrix.arch }}-apple-darwin
          path: ./bin/crystalline
  linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2
      - name: Build the static binary
        run: docker build -t crystalline .
      - name: Copy binary to host
        run: |
          docker run -v $PWD:/app/host --rm crystalline:latest cp ./bin/crystalline ./host/crystalline
      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: crystalline_x86_64-unknown-linux-musl
          path: ./crystalline
